# base

base theme for [`mkws(1)`](https://mkws.sh), you can use it to make
your own theme.

# Files

* `share/l.upphtml` - main layout file
* `share/s.uppcss` - `CSS` theme template
* `share/sitemap.uppxml` - `sitemap.xml` template
* `share/robots.upptxt` - `robots.txt` template
